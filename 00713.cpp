#include <bits/stdc++.h>

using namespace std;

int main()
{
	int TC,len,r;
	char s1[201],s2[201],res[201];
	scanf("%d",&TC);
	while(TC--)
	{
		memset(s1,-1,sizeof(s1)); memset(s2,-1,sizeof(s2)); memset(res,-1,sizeof(res));
		scanf("%s %s",s1,s2);
		len = max(strlen(s1),strlen(s2));
		r = 0;
		for(int i = 0;i <= 200;i++)
		{
		}
	}

	return 0;
}
